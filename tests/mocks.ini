[Command]
Command="
    const copy_ = global.copy;
    global.copy = function() {
        if (arguments.length === 0) {
            plugins.itemtests.keys('Ctrl+C');
        } else {
            copy_.apply(this, arguments);
        }
    }

    const paste_ = global.paste;
    global.paste = function() {
        plugins.itemtests.keys('Ctrl+V');
    }"
IsScript=true
Name=Mocks
