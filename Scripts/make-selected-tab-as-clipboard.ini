[Command]
Name=Make a selected tab as clipboard tab and put first item to clipboard
Command="
    const onTabSelected_ = global.onTabSelected;
    global.onTabSelected = function () {
        settings('clipboard_tab', selectedTab());
        select(0);
        return onTabSelected_();
    }"
Separator=" "
IsScript=true
Icon=\xf070