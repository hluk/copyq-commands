[Command]
Command="
    copyq:
    var mime = 'application/x-copyq-hidden'
    if ( dataFormats().indexOf(mime) != -1 )
      removeData(mime)
    else
      setData(mime, '')"
Icon=\xf070
InMenu=true
Name=Hide/Show Item Content
