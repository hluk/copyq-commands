[Command]
Name=Paste current date/time in ISO8601 format
Command="
    copyq:
    var dateTime = new Date();
    var isoDateTime = dateTime.toISOString();
    add(isoDateTime);
    copy(isoDateTime)
    paste()"
IsGlobalShortcut=true
Icon=\xf017
GlobalShortcut=meta+ctrl+t
