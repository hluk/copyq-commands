[Command]
Name=Join Text To One Line
Command="
    copyq:
    var text = str(input())
    text = text.replace(/[\\r\\n]/g, '');
    add(text);
    selectItems(0);
    "
Input=text/plain
InMenu=true
Icon=
Shortcut=ctrl+j
