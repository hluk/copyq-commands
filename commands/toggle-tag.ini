[Command]
Command="
    copyq:
    var tag = decodeURIComponent('important')
    if (plugins.itemtags.hasTag(tag))
        plugins.itemtags.untag(tag)
    else
        plugins.itemtags.tag(tag)"
Icon=\xf02b
InMenu=true
Name=Toggle tag \x201cimportant\x201d
